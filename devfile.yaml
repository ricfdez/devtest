schemaVersion: 2.1.0
metadata:
  name: test-ric
  #attributes:
 #controller.devfile.io/storage-type: ephemeral 
components:
  - name: frontend
    container:
      # image: quay.io/devfile/universal-developer-image:ubi8-3d26fec
      image: quay.io/williammr/devspaces:latest
      #image: quay.io/williammr/devspaces:0.0.1
     # memoryLimit: 2Gi -> testing with more resources.
      #memoryRequest: 1Gi -> testing with more resources.
      #cpuLimit: 500m -> testing with more resources.
      #cpuRequest: 30m -> testing with more resources.
